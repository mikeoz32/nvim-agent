# Команди nvim-agent

Повний список команд та гарячих клавіш

## 📋 Зміст

- [Основні команди](#основні-команди)
- [Режими роботи](#режими-роботи)
- [MCP інструменти](#mcp-інструменти)
- [Review Mode](#review-mode)
- [Історія чату](#історія-чату)
- [Гарячі клавіші](#гарячі-клавіші)

## Основні команди

### Чат

| Команда | Опис |
|---------|------|
| `:NvimAgentChat` | Відкрити/закрити вікно чату |
| `:NvimAgentAsk {питання}` | Швидко задати питання без відкриття чату |
| `:NvimAgentClear` | Очистити історію чату |
| `:NvimAgentSubmit` | Надіслати повідомлення (коли у вікні чату) |

### Робота з кодом

| Команда | Опис | Режим |
|---------|------|-------|
| `:NvimAgentExplain` | Пояснити виділений код | Visual |
| `:NvimAgentGenerate {опис}` | Згенерувати код | Normal |
| `:NvimAgentRefactor` | Відрефакторити виділений код | Visual |
| `:NvimAgentOptimize` | Оптимізувати виділений код | Visual |
| `:NvimAgentTest` | Згенерувати тести для виділеного коду | Visual |
| `:NvimAgentDoc` | Згенерувати документацію | Visual/Normal |
| `:NvimAgentFix` | Виправити помилки в коді | Visual/Normal |
| `:NvimAgentReview` | Зробити code review | Visual/Normal |

### Контекст

| Команда | Опис |
|---------|------|
| `:NvimAgentAddContext` | Додати поточний файл до контексту |
| `:NvimAgentAddSelection` | Додати виділений текст до контексту |
| `:NvimAgentClearContext` | Очистити контекст |
| `:NvimAgentShowContext` | Показати поточний контекст |

## Режими роботи

| Команда | Опис |
|---------|------|
| `:NvimAgentMode ask` | Переключити на режим Ask (запитання) |
| `:NvimAgentMode edit` | Переключити на режим Edit (редагування) |
| `:NvimAgentMode agent` | Переключити на режим Agent (автономний) |
| `:NvimAgentToggleMode` | Циклічно переключити режим |

### Ask (Запитання)

```
🤔 AI відповідає на питання та надає пояснення
```

Використовуйте для:
- Запитань про код
- Пояснень концепцій
- Пошуку помилок
- Отримання порад

### Edit (Редагування)

```
✏️  AI пропонує зміни коду
```

Використовуйте для:
- Рефакторингу
- Оптимізації
- Додавання функцій
- Виправлення помилок

### Agent (Автономний)

```
🤖 AI може самостійно виконувати задачі через MCP інструменти
```

Використовуйте для:
- Складних завдань
- Роботи з багатьма файлами
- Автоматизації
- Аналізу проекту

## MCP інструменти

Доступні в режимі **Agent**. 28 інструментів:

### Файли (5)

| Інструмент | Опис |
|------------|------|
| `read_file` | Читає вміст файлу |
| `write_file` | Записує вміст у файл |
| `list_directory` | Показує вміст директорії |
| `create_directory` | Створює директорію |
| `delete_file` | Видаляє файл |

### Проект (2)

| Інструмент | Опис |
|------------|------|
| `get_project_context` | Завантажує повний контекст проекту |
| `get_project_structure` | Показує структуру проекту (дерево) |

### LSP (9)

| Інструмент | Опис |
|------------|------|
| `lsp_diagnostics` | Отримує помилки та попередження |
| `lsp_definition` | Знаходить визначення символу |
| `lsp_references` | Знаходить всі використання символу |
| `lsp_hover` | Отримує документацію символу |
| `lsp_code_action` | Виконує code action |
| `lsp_rename` | Перейменовує символ |
| `lsp_format` | Форматує документ |
| `lsp_symbols` | Отримує всі символи документа |
| `lsp_workspace_symbols` | Шукає символи в робочому просторі |

### Редагування (4)

| Інструмент | Опис |
|------------|------|
| `search_text` | Шукає текст у файлах (grep) |
| `replace_text` | Замінює текст у файлах |
| `insert_text` | Вставляє текст у файл |
| `delete_lines` | Видаляє рядки з файлу |

### Буфери (4)

| Інструмент | Опис |
|------------|------|
| `get_current_buffer` | Отримує вміст поточного буфера |
| `list_buffers` | Показує всі відкриті буфери |
| `switch_buffer` | Переключається на інший буфер |
| `close_buffer` | Закриває буфер |

### Tree-sitter (1)

| Інструмент | Опис |
|------------|------|
| `parse_syntax` | Парсить синтаксис коду через tree-sitter |

### Команди (3)

| Інструмент | Опис |
|------------|------|
| `run_command` | Виконує vim команду |
| `run_shell` | Виконує shell команду |
| `get_register` | Отримує вміст vim register |

## Review Mode

### Команди

| Команда | Опис |
|---------|------|
| `:NvimAgentReviewMode [on\|off]` | Увімкнути/вимкнути review mode |
| `:NvimAgentReviewChanges` | Відкрити вікно перегляду змін |
| `:NvimAgentAcceptAll` | Прийняти всі зміни |
| `:NvimAgentDiscardAll` | Відхилити всі зміни |
| `:NvimAgentChangesStats` | Показати статистику змін |

### Гарячі клавіші у preview вікні

| Клавіша | Дія |
|---------|-----|
| `a` | Accept - прийняти поточну зміну |
| `d` | Discard - відхилити поточну зміну |
| `A` | Accept All - прийняти всі зміни |
| `D` | Discard All - відхилити всі зміни |
| `n` | Next - наступна зміна |
| `p` | Previous - попередня зміна |
| `q` або `Esc` | Quit - закрити вікно |

### Workflow

```vim
" 1. Увімкнути review mode
:NvimAgentReviewMode on

" 2. Попросити AI зробити зміни (в режимі Agent)
:NvimAgentMode agent
:NvimAgentChat
> Додай обробку помилок у всі функції

" 3. Переглянути зміни
:NvimAgentReviewChanges

" 4. Прийняти/відхилити через a/d
" або прийняти всі через A

" 5. Переглянути статистику
:NvimAgentChangesStats
```

## Історія чату

| Команда | Опис |
|---------|------|
| `:NvimAgentSaveChat {name}` | Зберегти поточну розмову |
| `:NvimAgentLoadChat {name}` | Завантажити збережену розмову |
| `:NvimAgentListChats` | Показати всі збережені розмови |
| `:NvimAgentDeleteChat {name}` | Видалити збережену розмову |

## Гарячі клавіші

### За замовчуванням

| Клавіша | Дія | Режим |
|---------|-----|-------|
| `<leader>cc` | Відкрити чат | Normal |
| `<leader>ca` | Задати швидке питання | Normal |
| `<leader>ce` | Пояснити код | Visual |
| `<leader>cg` | Згенерувати код | Normal |
| `<leader>cr` | Відрефакторити код | Visual |
| `<leader>co` | Оптимізувати код | Visual |
| `<leader>ct` | Згенерувати тести | Visual |
| `<leader>cd` | Згенерувати документацію | Visual/Normal |
| `<leader>cf` | Виправити помилки | Visual/Normal |
| `<leader>cm` | Переключити режим | Normal |

### У вікні чату

| Клавіша | Дія |
|---------|-----|
| `<CR>` (Enter) | Надіслати повідомлення |
| `<C-c>` або `Esc` | Закрити чат |
| `<C-l>` | Очистити історію |
| `<C-k>` | Прокрутити вгору |
| `<C-j>` | Прокрутити вниз |
| `i` | Увійти в режим вводу |

### Кастомні клавіші

Додайте у вашу конфігурацію:

```lua
require('nvim-agent').setup({
    keymaps = {
        toggle_chat = "<leader>ac",
        explain_code = "<leader>ae",
        generate_code = "<leader>ag",
        refactor_code = "<leader>ar",
        optimize_code = "<leader>ao",
        test_code = "<leader>at",
        doc_code = "<leader>ad",
        fix_code = "<leader>af",
        toggle_mode = "<leader>am",
        review_changes = "<leader>av",
    }
})
```

## Налаштування команд

### Створити команду для конкретного завдання

```lua
-- У вашому init.lua
vim.api.nvim_create_user_command('MyCustomAI', function(opts)
    require('nvim-agent.chat').send_message(
        "Виконай моє завдання: " .. opts.args,
        { mode = 'agent' }
    )
end, { nargs = '+' })
```

### Автоматичні команди

```lua
-- Автоматично увімкнути review mode для великих проектів
vim.api.nvim_create_autocmd("VimEnter", {
    callback = function()
        local file_count = vim.fn.system("fd -t f | wc -l")
        if tonumber(file_count) > 100 then
            require('nvim-agent.mcp').review_mode = true
            print("👁️  Review mode увімкнено (великий проект)")
        end
    end
})

-- Автоматично додавати поточний файл до контексту
vim.api.nvim_create_autocmd("BufEnter", {
    pattern = "*.lua",
    callback = function()
        require('nvim-agent.context').add_file(vim.fn.expand('%'))
    end
})
```

## Швидкі сніпети

### Найпоширеніші команди

```vim
" Швидкий рефакторинг
:'<,'>NvimAgentRefactor

" Пояснення функції під курсором
:NvimAgentExplain

" Генерація тестів для файлу
:NvimAgentTest

" Повний аналіз проекту
:NvimAgentMode agent
:NvimAgentChat
> Проаналізуй структуру проекту та запропонуй покращення

" Review mode workflow
:NvimAgentReviewMode on
:NvimAgentChat
> Додай логування у всі критичні функції
:NvimAgentReviewChanges
```

## Додаткові ресурси

- [README.md](../README.md) - Загальна інформація
- [MCP_TOOLS.md](MCP_TOOLS.md) - Детальна документація MCP інструментів
- [MCP_QUICKSTART.md](MCP_QUICKSTART.md) - Швидкий старт з MCP
- [VISUAL_FEEDBACK.md](VISUAL_FEEDBACK.md) - Візуальний зворотній зв'язок
- [REVIEW_MODE.md](REVIEW_MODE.md) - Детальна документація Review Mode
- [doc/nvim-agent.txt](../doc/nvim-agent.txt) - Vim help документація

---

**Використовуйте nvim-agent максимально ефективно! 🚀**
