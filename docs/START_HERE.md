# 🚀 Швидкий запуск для тестування

## 1. Запустити Neovim

```powershell
cd d:\work\nvim-agent
.\run_tests.ps1 test
```

Або напряму:
```powershell
nvim -u test_config.lua
```

## 2. Перевірити завантаження

У Neovim виконайте:
```vim
:lua print("nvim-agent loaded:", require('nvim-agent').version or 'yes')
```

## 3. Тест inline кнопок (найцікавіше! 🎯)

```vim
<Space>t4
```

Це створить mock зміну і покаже inline кнопки. Потім натисніть:
- `ga` - Accept (прийняти)
- `gd` - Discard (відхилити)
- `Enter` - Accept (альтернатива)

## 4. Відкрити чат

```vim
<Space>cc
```

## 5. Переглянути інфо

```vim
<Space>t5
```

## 6. Запустити автоматичні тести

```powershell
.\run_tests.ps1 test-auto
```

---

**Готово!** Більше деталей в [QUICKSTART.md](QUICKSTART.md) та [TESTING.md](TESTING.md)
