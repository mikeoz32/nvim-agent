# Швидкий старт з MCP в nvim-agent

## Що дає MCP?

MCP дозволяє AI виконувати дії замість того щоб тільки давати поради. Наприклад:

**Без MCP (режим Ask/Edit):**
```
Користувач: "Знайди всі TODO коментарі та створи issues"
AI: "Ось команда яку треба виконати: :vimgrep /TODO/j **/*.lua"
```

**З MCP (режим Agent):**
```
Користувач: "Знайди всі TODO коментарі та створи issues"
AI: 
🔧 Шукаю TODO коментарі...
✅ Знайдено 15 TODO
🔧 Створюю файл ISSUES.md...
✅ Створено список issues

Готово! Створено ISSUES.md з 15 задачами для виконання.
```

## Крок 1: Увімкнення MCP

MCP увімкнено за замовчуванням. Якщо потрібно, перевірте конфігурацію:

```lua
require('nvim-agent').setup({
    mcp = {
        enabled = true,  -- Увімкнено за замовчуванням
    }
})
```

## Крок 2: Переключення в режим Agent

MCP працює **тільки в режимі Agent**. Для переключення:

- Натисніть `<leader>cm` (за замовчуванням)
- Або в чаті натисніть `<C-m>`
- Режим відображається в заголовку вікна: `nvim-agent [Agent]`

## Крок 3: Використання

Просто попросіть AI зробити щось:

### 🚀 Швидкий контекст проекту

**Найкращий спосіб почати роботу - завантажити весь проект:**

```
Ви: "Покажи що у проекті"

AI: 🔧 get_project_structure()

📁 my-nvim-plugin
├── 📁 lua
│   └── 📁 my-plugin
│       ├── 📄 init.lua (2.1KB)
│       ├── 📄 config.lua (1.5KB)
│       └── 📄 utils.lua (3.2KB)
├── 📁 plugin
│   └── 📄 my-plugin.vim (0.8KB)
├── 📄 README.md (4.2KB)
└── 📄 LICENSE (1.1KB)

📊 15 файлів, 5 директорій
🎯 Тип: Neovim plugin
```

**Або завантажити повний контекст з кодом:**

```
Ви: "Додай нову команду для форматування"

AI: 🔧 get_project_context(include_content=true)
    ✅ Завантажено 15 файлів (45KB)
    📊 Neovim plugin
    📦 Модулі: init, config, utils, commands
    
    Аналіз:
    - Команди визначені у plugin/my-plugin.vim
    - Логіка у lua/my-plugin/init.lua
    - Утиліти форматування відсутні
    
    План:
    1. Створю lua/my-plugin/format.lua
    2. Додам команду :MyPluginFormat
    3. Інтегрую з LSP formatters
    
    Почати?
```

### Приклади простих задач

```
"Прочитай файл README.md"
"Знайди всі функції з назвою test_"
"Створи файл utils.lua з helper функціями"
"Покажи всі помилки в поточному файлі"
"Виконай тести через npm test"
```

### Приклади складних задач

```
"Знайди всі unused змінні та видали їх"
"Створи документацію для всіх публічних функцій"
"Відрефактори функції довші за 50 рядків"
"Знайди та виправ всі TODO коментарі"
"Створи тести для всіх функцій в src/"
```

## Доступні інструменти

### 📁 Робота з файлами
- `read_file` - Читає файл
- `write_file` - Створює/змінює файл
- `open_file` - Відкриває файл у буфері
- `find_files` - Шукає файли за патерном
- `grep_search` - Шукає текст у файлах

### 🔍 LSP інтеграція
- `get_diagnostics` - Показує помилки/попередження
- `goto_definition` - Знаходить визначення символу
- `find_references` - Знаходить посилання на символ
- `get_hover_info` - Отримує документацію символу
- `get_signature_help` - Підказка параметрів функції
- `get_document_symbols` - Список символів у файлі
- `rename_symbol` - Перейменовує символ скрізь
- `get_code_actions` - Отримує швидкі виправлення
- `format_code` - Форматує код

### ✏️ Редагування
- `insert_text` - Вставляє текст
- `delete_lines` - Видаляє рядки
- `replace_text` - Замінює текст (regex)
- `get_selection` - Отримує вибраний текст

### 📋 Буфери
- `list_buffers` - Список відкритих буферів
- `create_buffer` - Створює новий буфер
- `save_buffer` - Зберігає буфер
- `close_buffer` - Закриває буфер

### 🎯 Treesitter
- `get_treesitter_nodes` - Аналізує структуру коду

### ⚡ Команди
- `execute_command` - Виконує Neovim команду
- `execute_shell` - Виконує shell команду
- `execute_macro` - Виконує Vim макрос

## Що AI НЕ може робити

- Використовувати tools в режимах Ask або Edit
- Виконувати інтерактивні команди (які потребують вводу)
- Читати файли поза робочою директорією (без налаштування)
- Виконувати небезпечні системні операції (rm -rf тощо - залежить від AI)

## Налаштування безпеки

### Обмеження інструментів

Якщо не хочете щоб AI виконував shell команди:

```lua
require('nvim-agent').setup({
    mcp = {
        enabled = true,
        tools = {
            execute_shell = false,  -- Заборонити shell
        }
    }
})
```

### Рекомендації

1. ✅ Використовуйте git для контролю змін
2. ✅ Перевіряйте що робить AI перед важливими змінами  
3. ✅ Починайте з простих задач щоб зрозуміти як працює
4. ❌ Не давайте AI доступ до production баз даних
5. ❌ Не зберігайте паролі та токени в коді

## Порівняння режимів

| Режим | Tools | Коли використовувати |
|-------|-------|---------------------|
| **Ask** | ❌ | Запитання, пояснення коду |
| **Edit** | ❌ | Пропозиції змін коду |
| **Agent** | ✅ | Автоматизація задач |

## Troubleshooting

### AI не використовує tools

**Проблема:** AI відповідає але не виконує дій

**Рішення:**
1. Перевірте що ви в режимі Agent (`<leader>cm`)
2. Перевірте що MCP увімкнено в конфігурації
3. Спробуйте явно попросити "використовуй tools"

### Помилка виконання tool

**Проблема:** `❌ tool_name: помилка`

**Рішення:**
1. Перевірте логи: `:lua vim.print(vim.fn.stdpath("cache") .. "/nvim-agent.log")`
2. Перевірте права доступу до файлів
3. Перевірте що шлях до файлу правильний

### AI виконує багато tools

**Проблема:** AI викликає занадто багато інструментів

**Рішення:**
1. Будьте більш конкретні в запиті
2. Розбийте задачу на менші частини
3. Використайте режим Edit для пропозицій замість Agent

## Наступні кроки

1. Прочитайте повну документацію: [MCP_TOOLS.md](MCP_TOOLS.md)
2. Експериментуйте з простими задачами
3. Налаштуйте зовнішні MCP сервери (опціонально)
4. Приєднуйтесь до обговорень в Issues

## Корисні команди

```vim
:NvimAgentChat           " Відкрити чат
:NvimAgentMode           " Показати поточний режим
:NvimAgentCycleMode      " Переключити режим
:lua require('nvim-agent.mcp').list_tools()  " Список tools
```

---

**Пам'ятайте:** Agent режим - це потужний інструмент. Використовуйте його розумно! 🚀
