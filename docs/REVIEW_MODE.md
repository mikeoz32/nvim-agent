# Review Mode - Перегляд та контроль змін

## Що це?

**Review Mode** - це інтерактивний режим перегляду змін, згенерованих AI агентом. Працює як **Accept/Discard** у VS Code Copilot Chat!

## Як це працює?

### 1. Увімкнення Review Mode

```vim
:NvimAgentReviewMode on
```

Або через API:
```lua
require('nvim-agent.mcp').review_mode = true
```

### 2. Робота з AI

Коли Review Mode увімкнено, всі зміни від AI не застосовуються одразу, а **додаються до черги** для перегляду:

```
🧑 Ви: Створи функцію для валідації email

🤖 AI: Виконую 2 інструменти...

  💾 Записую файл: src/validators.js
  ✅ Файл збережено (очікує перегляду)

  📄 Створюю буфер: tests/validators.test.js
  ✅ Буфер створено (очікує перегляду)

  💭 Аналізую результати...

🤖 AI: Створено функцію validateEmail() та тести!

📋 Є 2 зміни для перегляду. Використайте :NvimAgentReviewChanges
```

### 3. Перегляд змін

```vim
:NvimAgentReviewChanges
```

Відкриється інтерактивне вікно з diff:

```diff
=== Новий файл: src/validators.js ===

+ export function validateEmail(email) {
+   const regex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
+   return regex.test(email);
+ }
```

**Кнопки управління:**
```
[a]ccept  [d]iscard  [A]ccept All  [D]iscard All  [n]ext  [p]rev  [q]uit
```

### 4. Управління змінами

| Дія | Клавіша | Опис |
|-----|---------|------|
| **Accept** | `a` | Прийняти поточну зміну |
| **Discard** | `d` | Відхилити поточну зміну |
| **Accept All** | `A` | Прийняти всі зміни |
| **Discard All** | `D` | Відхилити всі зміни |
| **Next** | `n` | Наступна зміна |
| **Previous** | `p` | Попередня зміна |
| **Quit** | `q` або `Esc` | Закрити (зміни залишаються в черзі) |

## Типи змін

### 1. Створення файлу (`FILE_CREATE`)

```diff
=== Новий файл: src/utils/helper.js ===

+ function formatDate(date) {
+   return date.toISOString();
+ }
```

### 2. Модифікація файлу (`FILE_MODIFY`)

```diff
=== Зміни у файлі: src/app.js ===

  import express from 'express';
+ import { validateEmail } from './validators.js';
  
  const app = express();
  
  app.post('/signup', (req, res) => {
-   // TODO: validate email
+   if (!validateEmail(req.body.email)) {
+     return res.status(400).json({ error: 'Invalid email' });
+   }
    // ...
  });
```

### 3. Видалення файлу (`FILE_DELETE`)

```diff
=== Видалення файлу: src/old-code.js ===

⚠️  Файл буде видалено
```

### 4. Вставка тексту (`TEXT_INSERT`)

```diff
=== Вставка тексту на лінії 45 ===

+ // TODO: Add error handling
+ try {
+   processData();
+ } catch (error) {
+   console.error(error);
+ }
```

### 5. Заміна тексту (`TEXT_REPLACE`)

```diff
=== Заміна тексту (лінії 10-15) ===

Старий текст:
- const result = data.filter(x => x > 10);

Новий текст:
+ const result = data.filter(item => item.value > 10);
```

## Приклади використання

### Базовий workflow

```vim
" 1. Увімкнути review mode
:NvimAgentReviewMode on

" 2. Попросити AI зробити зміни
:NvimAgentChat
> Додай обробку помилок у всі функції

" 3. Переглянути зміни
:NvimAgentReviewChanges

" 4. Прийняти потрібні, відхилити непотрібні
" (використовуючи a/d в preview вікні)

" 5. Переглянути статистику
:NvimAgentChangesStats

" 6. Вимкнути review mode
:NvimAgentReviewMode off
```

### Швидке прийняття всіх змін

```vim
" Якщо впевнені у всіх змінах
:NvimAgentAcceptAll
```

### Швидке відхилення всіх змін

```vim
" Якщо передумали
:NvimAgentDiscardAll
```

## Команди

| Команда | Опис |
|---------|------|
| `:NvimAgentReviewMode [on/off]` | Увімкнути/вимкнути review mode |
| `:NvimAgentReviewChanges` | Відкрити вікно перегляду змін |
| `:NvimAgentAcceptAll` | Прийняти всі зміни |
| `:NvimAgentDiscardAll` | Відхилити всі зміни |
| `:NvimAgentChangesStats` | Показати статистику змін |

## Конфігурація

```lua
require('nvim-agent').setup({
    mcp = {
        review_mode_default = false,  -- Увімкнути review mode за замовчуванням
        auto_show_review = true,      -- Автоматично показувати preview після змін
    }
})
```

## Гарячі клавіші

Додайте власні хоткеї:

```lua
vim.keymap.set('n', '<leader>cr', ':NvimAgentReviewChanges<CR>', 
    { desc = 'Review AI changes' })
vim.keymap.set('n', '<leader>ca', ':NvimAgentAcceptAll<CR>',
    { desc = 'Accept all AI changes' })
vim.keymap.set('n', '<leader>cd', ':NvimAgentDiscardAll<CR>',
    { desc = 'Discard all AI changes' })
```

## Підсвічування

Review вікно використовує стандартні diff highlight groups:

```vim
" Нові рядки (зелений)
highlight DiffAdd guibg=#2d4d2d guifg=#98c379

" Видалені рядки (червоний)
highlight DiffDelete guibg=#4d2d2d guifg=#e06c75

" Заголовки (синій)
highlight Title guifg=#61afef gui=bold

" Попередження (жовтий)
highlight WarningMsg guifg=#e5c07b
```

## Особливості

### Автоматичний backup

Всі зміни зберігають **оригінальний стан** файлу:

- Для нових файлів - зберігається чи існував файл
- Для модифікацій - зберігається весь вміст буфера
- Для видалень - зберігається вміст файлу

### Безпечне undo

Якщо ви випадково прийняли зміну, можна відмінити через vim undo (`u`).

### Стек змін

Зміни зберігаються у стеку з індексом:
- Можна переглядати історію змін
- Підтримується навігація вперед/назад
- Показується прогрес (2/5, 3/5 тощо)

### Перевірка перед видаленням

Перед видаленням файлу показується попередження:

```
⚠️  УВАГА: Файл буде видалено!
    
src/old-module.js (152 lines)

Ви впевнені? [a]ccept для підтвердження
```

## Порівняння з VS Code

| Можливість | nvim-agent | VS Code Copilot |
|------------|------------|-----------------|
| Accept/Discard | ✅ | ✅ |
| Accept All | ✅ | ✅ |
| Discard All | ✅ | ✅ |
| Diff preview | ✅ | ✅ |
| Навігація змінами | ✅ (n/p) | ⚠️ Обмежена |
| Backup/Undo | ✅ Повний | ⚠️ Базовий |
| Статистика змін | ✅ | ❌ |
| Типів змін | ✅ 5 типів | ⚠️ 2-3 типи |
| Українська мова | ✅ | ❌ |

## Best Practices

### 1. Завжди переглядайте зміни перед прийняттям

```vim
" Погано
:NvimAgentAcceptAll

" Добре
:NvimAgentReviewChanges
" ... переглядаєте кожну зміну ...
```

### 2. Використовуйте review mode для складних задач

```vim
" Для простих питань - review mode не потрібен
:NvimAgentReviewMode off
> Поясни цю функцію

" Для рефакторингу - увімкніть review mode
:NvimAgentReviewMode on
> Відрефактори весь проект з async/await на promises
```

### 3. Перевіряйте статистику

```vim
:NvimAgentChangesStats

📊 Статистика змін:

Всього: 15
✅ Застосовано: 8
⏳ Очікують: 7

За типом:
  • file_modify: 10
  • file_create: 3
  • text_replace: 2
```

### 4. Зберігайте зміни частинами

```vim
" Не приймайте всі зміни одразу
" Працюйте поетапно:

" 1. Прийміть зміни у тестах
:NvimAgentReviewChanges
" ... accept тільки test файли ...

" 2. Перевірте чи тести проходять
:!npm test

" 3. Якщо ОК - прийміть решту
:NvimAgentReviewChanges
```

## Troubleshooting

### Зміни не показуються

```vim
" Перевірте чи увімкнено review mode
:lua print(require('nvim-agent.mcp').review_mode)
" має бути true

" Увімкніть якщо false
:NvimAgentReviewMode on
```

### Preview вікно не відкривається

```vim
" Перевірте чи є зміни
:NvimAgentChangesStats

" Якщо Total: 0, зробіть зміни через AI
```

### Зміна не застосовується

```vim
" Перевірте логи
:messages

" Або детальні логи
:lua vim.print(require('nvim-agent.utils').get_logs())
```

## FAQ

**Q: Чи можна використовувати review mode постійно?**  
A: Так! Встановіть `review_mode_default = true` у конфігурації.

**Q: Що станеться якщо закрити Neovim з непереглянутими змінами?**  
A: Зміни будуть втрачені. Спочатку прийміть або відхиліть їх.

**Q: Чи можна відмінити Accept після застосування?**  
A: Так, через vim undo (`u`) у відповідному буфері.

**Q: Скільки змін можна зберігати в стеку?**  
A: Необмежено, але рекомендується переглядати регулярно.

**Q: Чи працює з external MCP servers?**  
A: Наразі тільки з вбудованими Neovim tools.

---

**Контролюйте кожну зміну від AI! 🎯**
